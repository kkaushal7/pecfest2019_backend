import React, {Component} from 'react';

class Events extends Component {
    render() {
        return (
            <h1>
                This is Events
            </h1>
        );
    }
}

export default Events;